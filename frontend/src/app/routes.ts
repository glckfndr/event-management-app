export const APP_ROUTES = {
  root: "/",
  events: "/events",
  eventDetails: "/events/:id",
  createEvent: "/events/create",
  myEvents: "/my-events",
  login: "/login",
  register: "/register",
  wildcard: "*",
} as const;
